spring:
  profiles:
    include:
      config-log,
      config-oss-minio,
      config-oss-minio-${activeEnvironmentName},
      config-server,
      config-spring
    active: environment-${activeEnvironmentName}